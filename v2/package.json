{
  "name": "tvclone-v2-server",
  "version": "2.0.0",
  "private": true,
  "description": "TV-Clone v2 — serves the Svelte editorial UI and proxies the API to the v1 backend.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "build": "cd frontend && npm install && npm run build"
  },
  "dependencies": {
    "express": "^4.21.2",
    "http-proxy-middleware": "^3.0.3"
  }
}
